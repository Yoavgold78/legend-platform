{
  "name": "@legend/types",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --no-error-on-unmatched-pattern || echo 'No files to lint'"
  },
  "devDependencies": {
    "@legend/config": "*",
    "typescript": "^5.4.0"
  }
}
