# Generate a 32-character secret: openssl rand -hex 32
# Or use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH0_SECRET='CHANGE_THIS_TO_A_RANDOM_32_CHAR_SECRET'

# Shell application URL
AUTH0_BASE_URL='http://localhost:3000'

# Auth0 tenant domain (get from Auth0 Dashboard)
AUTH0_ISSUER_BASE_URL='https://your-tenant.auth0.com'

# Auth0 Application credentials (get from Auth0 Dashboard → Applications)
AUTH0_CLIENT_ID='your-client-id-here'
AUTH0_CLIENT_SECRET='your-client-secret-here'

# Optional: Audience for API access (will be needed in Story 1.3)
# AUTH0_AUDIENCE='https://api.legend-platform.com'
